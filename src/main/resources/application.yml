# Spring properties
spring:
  datasource: 
    url: jdbc:mysql://localhost:3306/cinque
    username: root
    password: mysql
  jpa:
    show-sql: true
    
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

